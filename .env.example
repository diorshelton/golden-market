# API Configuration
#VITE_API_URL=http://localhost:8080/api/v1

# Development Notes:
# - Copy this file to .env for local development
# - Update VITE_API_URL to your deployed backend URL for production
# - All Vite env vars must be prefixed with VITE_ to be exposed to the client
```

---

**Also make sure `.env` is in your .gitignore:**

Check your `.gitignore` file includes:
```
# Environment variables
.env
.env.local
.env.production